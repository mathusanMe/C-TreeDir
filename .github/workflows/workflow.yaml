name: "C-Project Workflow"
on:
  push:
    branches:
      - "**"

jobs:
  build:
    runs-on: ubuntu-latest
    defaults:
      run:
        shell: bash -ileo pipefail {0}
        working-directory: ./
    steps:
      - name: "GCC VERSION"
        run: gcc --version
      - name: "Compile Project"
        run: make all
      - name: "Test"
        run: ./main test
