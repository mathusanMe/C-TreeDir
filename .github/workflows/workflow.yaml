name: "C-Project Workflow"
on:
  push:
    branches:
      - "**"

jobs:
  build:
    runs-on: ubuntu-latest
    defaults:
      run:
        shell: bash
        working-directory: ./
    steps:
      - name: "GCC VERSION"
        run: gcc --version
      - name: Path
        run: pwd
      - name: "Compile Project"
        run: make --file=Makefile all
      - name: "Test"
        run: ./main test
